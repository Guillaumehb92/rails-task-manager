<h1> My task </h1>

<p>
<%= link_to "Edit that task", edit_task_path %>
</p>


<h2><%= @task.id %>: <%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= if @task.completed==true then "Well done! You did it !" else "to do!" end %></p>


<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>

<p>
<%= link_to "Back to list", tasks_path %>
</p>
